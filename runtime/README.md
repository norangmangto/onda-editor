# runtime/

This directory will contain runtime assets for onda:
- Syntax highlighting queries (tree-sitter, Phase 1+)
- Default themes (Phase 1+)
- Built-in plugin stubs (Phase 2+)

In Phase 0, this directory is a placeholder. The `Config` crate handles the
minimal configuration needed at this stage.
