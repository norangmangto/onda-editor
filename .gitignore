/target
/bench/target
# Release bundle output (`cargo xtask bundle`)
/dist/
/.build
Cargo.lock
*.orig
.DS_Store
*.swp
*.swo

# Generated test fixtures (large files)
/bench/fixtures/

# Benchmark output
/bench/results/
# Note: BENCH_REPORT.md is a committed, published deliverable (Phase 0/5 exit
# criterion) — regenerate with `cargo run -p xtask -- bench-compare`.

# Editor state
/.onda/

# Reference-plugin build output
/plugins/*/target/
